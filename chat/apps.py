import chat
from django.apps import AppConfig


# Default configuration of app named chat

class ChatConfig(AppConfig):                   
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'chat'                                           
